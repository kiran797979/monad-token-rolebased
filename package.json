{
  "name": "monad-token-rolebased",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:monad": "hardhat run --network monad scripts/deploy.js",
    "lint": "eslint . || true"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.16.0",
    "eslint": "^8.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}
